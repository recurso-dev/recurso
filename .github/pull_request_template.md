<!-- Thanks for contributing to Recurso! -->

## What & why

<!-- What does this change do, and why? Link any related issue (Fixes #123). -->

## How it was tested

<!-- Commands run, scenarios covered, screenshots for UI changes. -->

- [ ] `go build ./...` and `go test ./...` pass
- [ ] `gofmt` clean and `golangci-lint run` passes (or `make hooks` installed)
- [ ] For API changes: `cmd/api/openapi.yaml` updated
- [ ] For DB changes: migration has matching up + down
- [ ] For frontend changes: `npm run build` passes

## Notes for reviewers

<!-- Anything to call out: trade-offs, follow-ups, areas needing extra eyes. -->
