# Base URL of the Recurso API, including the /v1 prefix.
# Leave unset in development — the Vite dev server proxies /v1, /auth,
# /portal/api, /portal/auth, and /checkout to http://127.0.0.1:8080.
# Set in production builds, e.g.:
# VITE_API_BASE_URL=https://api.recurso.dev/v1
